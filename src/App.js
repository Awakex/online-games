import React from "react";
import PropTypes from "prop-types";

const App = (props) => {
    return (
        <div>
            <h2>Working.</h2>
        </div>
    );
};

App.propTypes = {};

export default App;
